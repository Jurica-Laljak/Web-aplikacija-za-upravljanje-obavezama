export interface ToDoListInsert {
  name: string;
  // serialNumber: number;
  highLevelSort?: string;
  midLevelSort?: string;
  lowLevelSort?: string;
  // highlightdefault?: boolean;
  // defaultGroupId?: number;
}
